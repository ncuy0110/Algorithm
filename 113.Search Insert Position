class Solution {
    public int searchInsert(int[] a, int t) {
        int i=0;
        for(i=0; i<a.length-1; i++){
            if(a[i]>=t) break;
        }
        if(a[i]>=t) return i;
        return a.length;
    }
}
